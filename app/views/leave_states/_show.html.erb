<div class="row my-2">
  <div class="col-1">
    <%= link_to "Edit Data", edit_bn_nominal_roll_leave_state_path(@bn_nominal_roll, leave_states), class: "btn btn-sm btn-outline-primary"%>
  </div>
  <div class="col-2">
    <%= button_to "Delete Data", bn_nominal_roll_leave_state_path(@bn_nominal_roll, leave_states), method: :delete, class: "btn btn-sm btn-outline-primary" %>
  </div>
</div>
<table class="table table-bordered">
  <thead class="bg-light">
    <tr>
      <% leave_states.attributes.except("updated_at", "created_at", "id", "bn_nominal_roll_id").keys.each do | bn |%>
        <th> <%= bn.titleize %> </th>
      <% end %>
    </tr>
  </thead>
  <tbody>
    <tr>
      <% leave_states.attributes.except("updated_at", "created_at", "id", "bn_nominal_roll_id").values.each do | bn |%>
        <td><%= bn %> </td>
      <% end %>      
    </tr>
  </tbody>
</table>


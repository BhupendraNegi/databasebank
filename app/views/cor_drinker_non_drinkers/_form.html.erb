<%= form_with method: action, model: @cor_drinker_non_drinker, url: url do | f | %>
  <% if cor_drinker_non_drinker.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(cor_drinker_non_drinker.errors.count, "error") %> prohibited this cor_drinker_non_drinker from being saved:</h2>

      <ul>
        <% cor_drinker_non_drinker.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>
  <div class="row mb-3">
    <div class="col-2">
      <%= f.label :drinker_non_drinker, 'DRINKER / NON DRINKER:', class: "text-secondary" %>
    </div>
    <div class="col-4">
      <%= f.text_field :drinker_non_drinker, autofocus: true %>
    </div>
  </div>
  <div class="row mb-3">
    <div class="col-2">
      <%= f.label :remarks, 'REMARKS:', class: "text-secondary" %>
    </div>
    <div class="col-4">
      <%= f.text_area :remarks, cols: 50, rows: 5%>
    </div>
  </div>
  <div>
    <%= f.submit "Submit", class: "btn btn-sm btn-primary btn-block" %>
  </div>
<% end %>

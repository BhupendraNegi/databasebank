 <%= form_with method: action, model: @army_course, url: url do | f | %>
  <% if army_course.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(army_course.errors.count, "error") %> prohibited this army_course from being saved:</h2>

      <ul>
        <% army_course.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>
  <div class="row mb-3">
    <div class="col-2">
      <%= f.label :course, 'COURSE:', class: "text-secondary" %>
    </div>
    <div class="col-4">
      <%= f.text_field :course, autofocus: true %>
    </div>
  </div>
  <div class="row mb-3">
    <div class="col-2">
      <%= f.label :course_grading, 'COURSE GRADING:', class: "text-secondary" %>
    </div>
    <div class="col-4">
      <%= f.text_field :course_grading  %>
    </div>
  </div>
  <div class="row mb-3">
    <div class="col-2">
      <%= f.label :location, 'LOCATION:', class: "text-secondary" %>
    </div>
    <div class="col-4">
      <%= f.text_field :location  %>
    </div>
  </div>
  <div class="row mb-3">
    <div class="col-2">
      <%= f.label :remarks, 'REMARKS:', class: "text-secondary" %>
    </div>
    <div class="col-4">
      <%= f.text_area :remarks, cols: 50, rows: 5%>
    </div>
  </div>
  <div>
    <%= f.submit "Submit", class: "btn btn-sm btn-primary btn-block" %>
  </div>
<% end %>


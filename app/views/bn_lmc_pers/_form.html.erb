<%= form_with(model: @bn_lmc_per, url: {action: "create"}) do |form| %>
  <% if bn_lmc_per.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(bn_lmc_per.errors.count, "error") %> prohibited this bn_lmc_per from being saved:</h2>

      <ul>
        <% bn_lmc_per.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :BnNominalRoll_id, style: "display: block" %>
    <%= form.text_field :BnNominalRoll_id %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>

<%= form_with(model: parade_state_of_unit) do |f| %>
  <% if parade_state_of_unit.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(parade_state_of_unit.errors.count, "error") %> prohibited this parade_state_of_unit from being saved:</h2>

      <ul>
        <% parade_state_of_unit.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>
  <div class="row mb-3">
    <div class="col-2">
      <%= f.label :auth, 'AUTH:', class: "text-secondary" %>
    </div>
    <div class="col-4">
      <%= f.text_field :auth, autofocus: true %>
    </div>
  </div>
  <div class="row mb-3">
    <div class="col-2">
      <%= f.label :posted, 'POSTED:', class: "text-secondary" %>
    </div>
    <div class="col-4">
      <%= f.text_field :posted  %>
    </div>
  </div>
  <div class="row mb-3">
    <div class="col-2">
      <%= f.label :present, 'PRESENT:', class: "text-secondary" %>
    </div>
    <div class="col-4">
      <%= f.text_field :present  %>
    </div>
  </div>

  <div>
    <%= f.submit "Submit", class: "btn btn-sm btn-primary btn-block" %>
  </div>
<% end %>

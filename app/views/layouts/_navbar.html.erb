<nav class="navbar navbar-expand-sm bg-dark navbar-dark sticky-top py-2">
  <div class="container">    
    <div class="col-12">
      <div class="row justify-content-end">
        <div class="col-6 d-flex justify-content-start">
          <% if user_signed_in? %>
            <a class="navbar-brand" href="/home">Database Bank </a>
          <% else %>
            <a class="navbar-brand" href="/">Database Bank </a>
          <% end %>
        </div>
        <div class="col-6 d-flex justify-content-end">
          <% unless user_signed_in? %>
            <%= link_to "<button type='button' class='btn btn-sm btn-light mx-2'> <i class='fa fa-user-circle'></i> Login</button>".html_safe, new_user_session_path, class: 'py-1'%>
          <% else %>
            <%= link_to "<button type='button' class='btn btn-sm btn-light mx-2'> <i class='fa fa-gear'></i> Edit Account </button>".html_safe, edit_user_registration_path, class: 'py-1'%>
            <%= link_to "<button type='button' class='btn btn-sm btn-danger mx-2'> <i class='fa fa-sign-out'></i> Logout </button>".html_safe, destroy_user_session_path, method: :delete, class: 'py-1'%>
          <% end %>
        </div>
      </div>
    </div>
  </div>
</nav>
